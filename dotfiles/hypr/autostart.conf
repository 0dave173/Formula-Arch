# ============================================================
# Formula-Arch - Autostart
# ============================================================

exec-once = waybar
exec-once = mako

exec-once = swww init || true
exec-once = ~/.local/share/formula-arch/bin/set-wallpaper.sh || true
